apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels: 
    {{ include "dora.labels" $ | indent 4 }}
spec:
  ports:
    - name: metrics
      protocol: TCP
      port: 8090
      targetPort: 8090
  selector:
    {{ include "dora.labels" $ | indent 4 }}
