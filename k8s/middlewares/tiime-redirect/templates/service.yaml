apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-links-service
spec:
  selector:
    app: {{ .Release.Name }}-links
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
