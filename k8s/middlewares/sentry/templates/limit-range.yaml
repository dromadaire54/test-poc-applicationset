apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit-range
spec:
  limits:
    - default:
        memory: 500Mi
      defaultRequest:
        cpu: 1000m
        memory: 500Mi
      type: Container
